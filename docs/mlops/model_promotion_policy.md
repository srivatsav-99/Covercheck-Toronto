# Model Promotion Policy

## Purpose

This document defines how candidate models are promoted to champion status in Toronto CoverCheck.

## Champion Model

The champion model is the model currently used by the scoring pipeline and production dashboard.

Champion location:

```text
models/registry/neighbourhood_collision/champion/